const { isTypedArray } = require("util/types")
const index = require("../index")

describe("Employee", () => {
    describe("promptManager", () => {
        it("should use inquirer to prompt a string", () => {
            const officeNumber = "office number"
            const obj = new index()
        })
    })
}
)